[project]
name = "ugrc-styles"
version = "0.1.0"
description = "Alternate MapLibre styles for UGRC's Lite vector basemap."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
    "requests>=2.32",
    "pillow>=10.4",
    "mini-racer>=0.12",
]

[project.scripts]
ugrc-styles = "ugrc_styles.build:main"

[dependency-groups]
dev = [
    "pytest>=8.3",
    "ruff>=0.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ugrc_styles"]

[tool.ruff]
line-length = 100
